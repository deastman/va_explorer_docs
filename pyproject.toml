[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "VA Explorer"
authors = [{name = "The MITRE Corporation", email = "verbal-autopsy@mitre.org"}]
dynamic = ["version", "description"]
